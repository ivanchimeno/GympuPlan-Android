package com.ivanchimeno.gympuplan;

/**
 * Possible schedule types. Used inside VPlanViewerActivity,
 * to keep track of which schedule type to use.
 */
public enum ScheduleType
{
    STUDENT,
    TEACHER
}
